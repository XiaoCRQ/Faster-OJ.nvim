# 发展规划

Faster-OJ.nvim 未来功能方向与路线图。

## 高优先级（差异化竞争力）

- [ ] **提交结果追踪** — 提交后轮询 OJ 状态页或 API，在通知中显示判定结果、运行时间、内存
- [ ] **自定义裁判器** — 支持浮点容差（`1e-6`）、用户提供的 checker 二进程序
- [ ] **交互式题目** — 双向管道通信，适配需要"读询问→输出回答→再读"的交互题
- [ ] **随机数据生成器** — `data=gen:PATH` 调用用户写的 generator，自动循环对拍到发现 WA 为止

## 中优先级（体验提升）

- [ ] **Side-by-side diff** — 逐行/逐 token 高亮输出差异，类似 `nvim -d`
- [ ] **用例性能记录** — 记录每次运行各用例的时间/内存，方便比对优化前后
- [ ] **代码片段库** — 比模板更细粒度，如 `fenwick` → 树状数组、`segtree` → 线段树，选中即插入
- [ ] **比赛模式** — 批量操作：一键运行整场比赛所有题、一键全部提交
- [ ] **Debug 快捷键** — `<leader>cdD` 以当前题目的输入自动启动 gdb/lldb

## 低优先级（锦上添花）

- [ ] **多文件题目支持** — 管理多个源文件，适配 AtCoder 库函数等题型
- [ ] **OJ API 直连** — 通过 Codeforces/AtCoder API 直接提交，不依赖浏览器插件
- [ ] **排名查看器** — 在 Neovim 内拉取实时比赛 standings
- [ ] **团队模式** — 共享 session，分工协作战队赛

## 已有功能深化

- [ ] **对拍自动终止** — 首次 WA 后自动停止；连续 N 轮 AC 后自动停止
- [ ] **UI 终端面板** — 挂载到运行中进程的 stdin/stdout，方便交互式调试

## 说明

- 各项在各自分组内按优先级大致排序
- 暂无截止日期或负责人分配，仅为方向性规划
- 合并到 main 后打勾标记
